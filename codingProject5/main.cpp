/*
#include <iostream>

#include <iostream>
#include <vector>
#include <queue>

#include "Graph.h"
#include "unitTest.h"
int main() {
    Graph graph(5);
    graph.addEdge(0, 1);
    graph.addEdge(0, 2);
    graph.addEdge(1, 3);
    graph.addEdge(2, 4);
    graph.addEdge(3, 4);

    graph.printBFSTree();
    graph.BFS(0);

    //graph.printShortest(); //0-4
    std::cout << std::endl;
    return 0;
}*/
